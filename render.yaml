services:
  - type: web
    name: hackpitch-ai
    env: node
    plan: free
    buildCommand: npm install && npm run db:generate && npm run db:init
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        value: file:./dev.db
      - key: FEATHERLESS_API_KEY
        sync: false
